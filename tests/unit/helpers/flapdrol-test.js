import { flapdrol } from '../../../helpers/flapdrol';
import { module, test } from 'qunit';

module('Unit | Helper | flapdrol');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = flapdrol(42);
  assert.ok(result);
});
