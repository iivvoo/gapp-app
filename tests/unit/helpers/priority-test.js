import { priority } from '../../../helpers/priority';
import { module, test } from 'qunit';

module('Unit | Helper | priority');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = priority(42);
  assert.ok(result);
});
